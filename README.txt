Para a compilação do programa foi utilizado o java 17, no sistema operacional Linux,
para rodar via terminal pode ser usado o script "run.sh", caso queira rodar via eclipse
basta rodar o controller/Controlador.
O arquivo de entrada segue a estrutura passada pela professor em aula.